command:
  php -m:
    exit-status: 0
    stdout:
      - /^intl$/
      - /^zip$/
      - /^gd$/
      - /^imagick$/
      - /^vips$/
      - /^xdebug$/
  node -v:
    exit-status: 0

file:
  /usr/local/etc/php/conf.d/99-test.ini:
    exists: true
    mode: "0644"

process:
  nginx:
    running: true

http:
  http://127.0.0.1:80/:
    status: 200
